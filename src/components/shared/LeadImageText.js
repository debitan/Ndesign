import styled from 'styled-components'

const LeadImageText = styled('div')`
    font-size: 16px;

    @media (min-width: 900px) {
        font-size: 20px;
        line-height: 2;
        text-align: left;
    }
`

export default LeadImageText
